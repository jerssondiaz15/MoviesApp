<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/im_cp_left"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/ic_left"
        android:visibility="invisible"
        android:tint="@color/black"
        app:layout_constraintEnd_toStartOf="@id/tv_cp_page_num"
        app:layout_constraintTop_toTopOf="@id/tv_cp_page_num"
        app:layout_constraintBottom_toBottomOf="@id/tv_cp_page_num"/>

    <TextView
        android:id="@+id/tv_cp_page_num"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1"
        android:textSize="28sp"
        android:textColor="@color/black"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ImageView
        android:id="@+id/im_cp_right"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/ic_right"
        android:tint="@color/black"
        app:layout_constraintStart_toEndOf="@id/tv_cp_page_num"
        app:layout_constraintTop_toTopOf="@id/tv_cp_page_num"
        app:layout_constraintBottom_toBottomOf="@id/tv_cp_page_num"/>

</androidx.constraintlayout.widget.ConstraintLayout>